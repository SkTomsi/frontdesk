{
  "name": "@frontdesk/worker",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot --no-clear-screen --env-file ../../.env ./src/index.ts",
    "reindex": "bun --env-file ../../.env ./src/reindex.ts",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@frontdesk/ai": "workspace:*",
    "@frontdesk/db": "workspace:*",
    "@frontdesk/ingest": "workspace:*",
    "@frontdesk/logger": "workspace:*",
    "@frontdesk/queue": "workspace:*",
    "@frontdesk/storage": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
